<% unless current_user?(@author) %>
	<div id="follow_form">
		<% if current_user.following?(@author) %>
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>