<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls='navbarToggler' aria-expanded='false' aria-label='Toggle navigation'>
    <span class="navbar-toggler-icon"></span>
  </button>
  <%= link_to "gossip project", root_path, class: "navbar-brand"%>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ml-lg-auto">
      <li class="nav-item">
        <%= link_to "HOME", root_path, class: "nav-link"%>
      </li>
      <% if logged_in? %>
        <li class="nav-item">
          <%= link_to "create gossip", new_gossip_path, class: "btn btn-primary" %>  
        </li>
        <li class="nav-item">
          <%= link_to "conversations", conversations_path, class: "nav-link" %>  
        </li>
        
        <div class="dropdown show">
          <a class="btn btn-light dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Account
          </a>

          <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
            <%= link_to "Profile", author_path(current_user), class: "dropdown-item" %>
            <%= link_to "Settings", edit_author_path(current_user), class: "dropdown-item" %>
            <div class="dropdown-divider"></div>
            <%= link_to "Log out", session_path(current_user), method: :delete, class: "dropdown-item"%>
          </div>
        </div>
      
      <% else %>
        <li class="nav-item">
          <%= link_to "Log In", new_session_path, class: "nav-link" %>  
        </li>
      <% end %>
    </ul>
  </div>
</nav>
